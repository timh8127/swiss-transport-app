# Docker Compose for local development
# Run with: docker compose up

version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      # Separate API keys for each OTD service
      - OTD_OJP_API_KEY=${OTD_OJP_API_KEY}
      - OTD_GTFSRT_API_KEY=${OTD_GTFSRT_API_KEY}
      - OTD_SIRI_SX_API_KEY=${OTD_SIRI_SX_API_KEY}
      - OTD_TRAFFIC_SITUATIONS_API_KEY=${OTD_TRAFFIC_SITUATIONS_API_KEY}
      - OTD_TRAFFIC_LIGHTS_API_KEY=${OTD_TRAFFIC_LIGHTS_API_KEY}
      # Server settings
      - PORT=8000
      - DEBUG=true
      - CORS_ORIGINS=http://localhost:3000,http://localhost:80
      - TIMEZONE=Europe/Zurich
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=http://localhost:8000
    ports:
      - "3000:80"
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

networks:
  default:
    name: swiss-transport-network
